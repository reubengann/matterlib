# Symbolic mathematics

The primary entry point is `spp`:

```python
from matterlib import spp
```

## Symbolic helpers

::: matterlib.symbolic

## `spp` namespace

The `spp` object is an instance of the namespace below. It exposes these
helpers and delegates unknown attributes to SymPy.

::: matterlib.symbolic._SympyPhys
    options:
      filters:
        - "!^_"
      members: true
      show_root_heading: false
